// Visible extents of the OS plane at z=0 for the fov-50 camera at z=5 with
// a 1.9/1.6 aspect — keeps the drawn cursor exactly under the real pointer.
export const SCREEN_WIDTH = 5.54
export const SCREEN_HEIGHT = 4.66

// Locally bundled pixel font — keeps the OS text retro and avoids a runtime
// CDN fetch for troika's default font.
export const OS_FONT = '/fonts/kenpixel.ttf'
